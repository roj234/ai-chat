3.10.8 (9/2)

已知问题和警告：
- 安卓端目前生成标题会抛出异常，会在 3.11.0 和后端的log数据堆积问题一起修复

Tools:
- Draw: 结果存入文件系统。
- TaskCreate: 允许创建已完成的任务。
- Glob: 通过 .ignore `dir/` 方式隐藏的目录，将会报告自身的名称，和隐藏子级的事实，使用`dir`方式完全隐藏。
- RunJS: 优化参数，加入CommonJS支持。
- Subagent:
    - 子代理可以不使用工具了。
    - 支持加载代理提示词中的工具。
    - 删除状态轮询工具。
    - 加入向子代理发送消息的能力。
    - 加入杀死子代理功能。
- Files:
    - 从工具参数中删除overwrite。现在由框架自行决定：读取或写入过的文件才能覆盖。
    - Write工具现在会将原始文件移动到垃圾箱（若开启），意味着文件系统本身已经有撤销能力。
- 加入异步通知：异步进程、异步子代理会在结束时通知主代理
    - 警告：子代理暂不支持异步通知，不建议嵌套使用异步子代理或异步进程，可能导致子代理提前返回

FS:
- 支持自动加载项目根目录下的AGENTS.md。
- 优化文件系统选择弹窗的标题
- 修复回收站无法放文件夹的bug
- 改进沙箱，修复一些bug并(在OPFS中)实现fs.open函数

注意：大部分以命令形式暴露的功能均为预览功能，命令本身可能会在之后被删除并转换为GUI。

UI:
- 加入锁定预设按钮
- 加入SPA窗口管理器
- 加入上下文使用率圆环
- 加入 /share 命令
- 消息角色和名称增加描边提高自定义背景下的可读性
- 实现了多选删除，你现在可以多选对话并删除它们。

- 重新设计附件上传UI
- 远程控制开关拆分为独立配置
- 优化打印预览，现在你可以使用Ctrl+P打印页面（到PDF），效果和其他软件的导出为图片差不多。
- 优化附件损坏提示
- 优化模型切换弹窗弹出的时机

- 修复加载动画过度消耗CPU的问题
- 修复上传大文件时页面卡顿的问题（BlobStream的Promise立即Resolve）
- 修复【文件传输助手】同步时报错【合并冲突】的问题
- 修复附件管理多选框在刷新后依然选中的问题
- 修复附件管理可能无法加载图片的bug
- 修复通过OPFS 读-改-写 超过2MB的文件时可能抛出异常的问题（浏览器特性）
- 修复了非思考模式不流式渲染的问题
- 修复打开包含大量图片的对话很慢的问题（串行等待网络）
- 修复编辑时切role会重置对content的修改的问题
- 修复更新后更新提示还在的bug（缓存）
- 修复某些场景菜单无法滚动的bug
- 修复某些场景侧边栏无法打开的bug
- 删除主界面不知所云的icon

Logic:
- 加入 loopBegin loopEnd conversation/load conversation/branch 事件
- 生成失败且没有任何内容的消息在regen前自动删除
- 系统消息不再被自动排序到开头，避免和分支管理器的冲突
- 在读取对话缓存时正确创建和销毁分支管理器
- 优化与MiniMax官方提供商的兼容性
- 修复分支对话删除时的bug并加入测试
- 修复能力探测【JSON Schema (部分)】总是不支持的bug
- 修复图片压缩算法的bug，加入图像长边和大小限制
- 修复工具调用不完整时也会调用renderer函数的问题
- 修复llama.cpp新版本的prefill会导致模型说的话被重复一遍的问题
- 加入toolRetryLimit简单工具调用重试方式

BasicRP:
- 为正则输入框加入语法检查和帮助外链
- [群友] 打开预设等编辑器时自动置顶
- 修复渲染正则传递了错误的楼层序号

APP:
- 使用代理Fetch解决安卓端的CORS问题
    - 之后后端版本也会加入

Backend:
- 加入文件系统测活（优化错误提示）
- 加入梁文谷定价机制
- 实现服务端的请求头伪装
- 优化节省SSE Proxy -> Client的网络流量
- 修复SSE Proxy响应时间计算的问题
- 为服务器地址加入校验，而不是直接接收

Misc:
- 开发模式启动时预构建服务端，以允许从 / 导入

3.9.0 (8/18)

支持编辑结构化思维链
修复文件缓存未使用绝对路径的bug
文件缓存现在使用LRU而不是WeakRef
支持Ctrl+V粘贴文件
修了一些预设问题
隔离模式下的消息也支持统计信息了
修复拖动上传的问题
修改同步服务js的名称
巨幅提升远程控制的UX易用性
分支对话现在也支持同步了
WS同步上传信息支持msgpack
客户端ID从随机数换成自增整数
修改一些文件的命名
隐藏文本补全模式下不生效的参数
使用我的ws2库替换ws

3.8.0 (8/16)

修复因缓存改进重复添加一些消息的bug
修复kvList缓存对象不共享的问题
删除的音频文件显示BlobDisplay组件
修复工具调用长度设置为1时会自动继续的问题
本地文件系统加入回收站功能
尝试修一下rg区
切换模型时发出警告，避免误操作
加入记住输入框插件
修复后端 ignore 不支持热更的 bug
修复/fsync时间返回有误的问题
修复偶尔工具调用结束后无法继续需要刷新的bug
修复后台对话开始也会导致前台对话滚到底部的bug
修复打开雷霆长思考时很卡的问题
增加Mountpoint工具的使用说明
[奶龙] 修复能力探测无法运行的bug
[3.7f] 修改工具调用编辑器的按钮颜色
[群友] 修复RP没法工作的bug，修复世界书激活问题，
[群友] 修复酒馆trim和random指令
[群友] 加入插件加载失败弹窗
加入事件总线，现在KV和KVS更新通过它发布，自动维护数据状态
[群友] 导出数据使用JSONL格式，同时向前兼容
KVList缓存换LRU
修复白天模式一部分颜色问题
[群友] 重新设计预设下拉菜单样式
[群友] 角色卡和世界书支持自动同步了
修复世界书不处理宏的问题
修复了后端更新性能回归

3.7.0 (8/14)

breaking:
[GPT5.6]删除了config.fs_server配置项，转为本地IndexedDB存储最近使用
- 同时为文件访问服务加入了datalist自动补全
- 如果你使用专有文件访问服务，手动执行 /fsreset 重新初始化文件访问

功能：
[GPT5.6]优化一些IndexedDB的错误处理
优化JSON流式解析性能
/setprompt命令改为/freezeprompt
修复/actime工具调用时间显示NaN的问题
修复和Moonshot API的兼容性问题
加入preset override功能

后端：
[GPT5.6]修复数据库连接关不掉的问题
后端的文件访问模式允许加载插件
修复msgpack schema压缩在上次重构后失效的问题
Shell命令返回执行时间
RunProgram支持传入环境变量
[GPT5.6]修复同时开启日志和ref模式时服务器崩溃的问题
修复了消息锁、同步的部分边缘情况
加入 --no-sandbox 开关禁用文件系统路径过滤
加入 --hide-user 开关隐藏用户名(partially, 不建议在linux下用)

工具：
grep移到 fs-common + ripgrep 节省流量
- 顺便行为与 rg 一致： *.js => **/*.js
修复Subagent找不到预设会抛异常的问题
新增_subagent_prompt预设名称可以设置子代理的公共系统提示词
- 同时子代理也会包含默认系统提示词了
- 修复了子代理数据同步过度请求网络、偶尔无法启动，偶尔报错等Bug
[GPT5.6]为Edit和Patch工具加入更详细的错误信息，以及自动缩进匹配
JSON编辑工具转为默认开启，我在EditJson中增加了类型断言，可以防止模型改变字段的类型
添加MCP时检测连通性
[V4F]修复偶尔MCP打勾但不加载工具的bug
修复Chart里面delimiter未起作用的问题
修复JS沙箱无法覆盖文件的bug
修复一些可能影响前缀缓存的问题
恢复Read工具读取图片的能力
SetTimeout工具支持被子代理使用。
增加写标准输入的工具
加入实验性微信机器人功能
禁用Files时自动重置文件系统选择

UI:
优化了工具审核的语义，现在主动暂停的工具调用也会显示审核对话框，允许拒绝而不需要手动编辑结果
regen不再自动执行工具
修复发起请求时会自动滚动到底部的问题
修复发消息不会自动滚动到底部的问题
修复同步服务已断开的错误信息未转义HTML的问题
修复弹窗中的换行消失的bug
修复删除分支消息之后leaf字段值错误导致消息不渲染的问题
不再为“你好”之类的请求生成标题
[GPT5.6]修复可复制虚拟列表Mixin有时失效的问题
生成时，网页标题会加【生成中】前缀
为消息加入了完整的缓存机制，加入了脏标记提升diff性能，并降低了导出极大量对话时的内存需求
修复获取写入锁之后界面闪烁一帧的bug
修复关闭再打开外观设置时透明效果消失(群友Miaji秀)
修复模型能力探测失败后一直显示失败和相关交互问题(群友Miaji秀)

BasicRoleplay插件:
加入了之前删除的递归激活世界书功能，大概是我用来炫技的，我写了个均摊开销（按每条消息算）约为O(1)的世界书匹配器
- 注意，修改世界书后需要点角色卡上的清除缓存按钮，否则是未定义行为
修复世界书不能点常驻和其他交互bug

实现了基于虚拟文件系统的世界书——然而我对此并不是非常感兴趣，因为我们有真正的文件系统了
- 尽管如此，你现在可以对这些字符串进行glob和grep了
- 你需要手动启用文件读写工具
- 我注入了一行提示词，告诉模型 ~/lorebook 的存在，剩下的就靠你了


3.3.1 (8/7)

### UI优化
- 一大堆
- 语法高亮不再硬换行，优化性能，优化代码
- 日志查看器的【总计】包含缓存的token
- 增加max思考级别
- 侧边栏可以拖拽调节宽度
- 优化了协议不兼容弹窗，你现在可以【无视风险，继续】了
- 加入耗时分析插件，使用 `/actime` 命令
- 修复PresetPanel中虚拟列表的排序问题
- Enter键可以点击弹窗的保存按钮了
- 更新JSON编辑器，加入SPA模式，支持安卓使用
- 修复请求日志中首token延迟的计算bug，原先只考虑网络延迟
- 编辑生成出错的消息时自动删除错误，它会被视为完整的消息（毕竟你编辑了）

### UI - 工具调用
- AskUser 工具支持多选了
- 工具调用加入耗时字段，方便未来做统计工具
- 修复Write和Edit工具在调用之前看不到Diff无法审核的问题
- 修复SetTimeout在禁用自动调用工具时工作异常的问题
- UpdateVariable可以完美撤销
- db文件系统会启用文件传输工具
- 切模型时regen将生成新分支
- 工具支持命名空间——允许注册同名工具，只要它们不同时暴露给模型
- 在后端容器模式下，告知AI工作目录的绝对路径
- 加入更好的Patch工具，并且默认启用，基于Unified Diff Hunk。

### 功能
- 修复IndexedDB数据库可能在启动时报错的问题
- 模型 cost 使用6位定点小数，不再是浮点（更新时自动迁移）
- 修复删除对话分支时可能删除不完全的bug
- 优化模型能力探测代码，优化 prefill 探测方式，支持流式/非流
- 子代理工具加入四种模型类型：inherit(默认, 回落) fast balanced precise
  - 你需要创建名称形如 `_subagent_fast` 的预设，否则回落到 inherit
  - 我感觉到预设这个平列表管理有些吃力，之后应该会重构
- 删除工具调用的default参数
- 删除冗余代码，并将JSON Schema能力合入agentLoop函数，使得RPG管线应用支持一切2.0来新增的特性，包括但不限于断线重连，多对话并行，多端同步等
  - 注意，由于SchemaRole是代码控制的，（现有的手动命令仅为PoC），因此不支持【继续生成】操作

### 后端
加入数据库相对地址引用，形如 @sse/v1 这样预设中就不需要保存完整域名，增加便携性
SSE Proxy 加入 llama.cpp 部分私有API的代理，支持 llamaCpp 插件
ws同步使用5分有效期的新生成受限域PAT避免原始PAT被记录在请求日志中
修复行为不一致【不再拒绝忽略目录的glob】
加入【消息引用】功能：SSE Proxy 后端缓存文本/图片消息，节省流量
ESM沙箱支持加载CommonJS了
后端文件系统允许使用`/tmp/`目录
支持多端同步流式生成，可以在流式生成时切换对话所有权而不丢失数据了！
- 需要 SSE Proxy 实现 tee()

### 警告：数据无价
- 过去版本流式生成时点击【获取编辑权限】，会直接导致生成中的那条对话丢失
- 现在我简单测试了一下，可以在生成中直接切写入所有者了，但是不保证完全没问题
- 同步流式生成的目的是更好 read 而不是为了 write
- 需要确保两端LLM端点配置相同（我还在考虑是否要同步整个 config 对象）

3.0.0 (7/31)

## 主要功能和改进

删除对话分支按钮、警示弹窗和配置项
- 用户消息加入【分支】按钮
- 助手消息加入【regen创建新分支】配置项
- 分支管理器在创建第一个分支时自动初始化，删除最后一个分支时自动销毁

加入远程控制功能（实验性）

支持录音输入

重构工具调用按钮
- 移除全局【工具启用】配置
- 默认为关闭，点击按钮时初始化为默认工具组
- 点击时打开工具配置抽屉

重构分支管理器，从链表转为DAG，make illegal states unrepresentable
- 只在分支点创建parent和children，减少内存/数据库开销
- 改用相对偏移，移除tricky的bm_dummy字段和检查
- 兼容之前版本（自动migration）

删除 BasicRoleplay 插件中所有未实现的功能
- 它们将被工具替代
- BasicRoleplay 将不再增加新功能，毕竟它只是 Basic

重构JS沙箱：强化安全性、兼容性的同时大幅提升性能
- RunJS 工具现在和 RPG 管线一样使用 safe-worker 沙箱
- 优化 Lockdown 堵上所有 RCE
- 支持动态构造模块、循环导入
- 加入 console 和文件系统访问能力，支持二进制。
- 加入大量可选模块实现 PDF DOCX XLSX PPTX 读写，实现无后端办公能力

### 工具
加入文件系统修改同步命令 /fsync
增加GPT5.6写的软件工程提示词模板（未使用）
支持【依赖】
加入定时任务能力
多媒体生成：删掉内置工作流模板，加补全，加格式验证和简介
文件操作：除了行号统一，删除所有的文件输入转换。
重构写保护：禁止运行，而不是控制工具存在，现在从 Plan 模式切换不会丢失缓存了。
重构上传/下载工具：支持二进制。
重构人类工具：只保留通用的 HumanInput 工具
骰子工具加入暗骰
修复模型喜欢在AskUser选项里写其它的问题
删掉 RunBackgroundProcess 改为 async 参数
Edit 返回修改后的行号
Chart 工具使用CSV文件，而不是内联数据了
glob/grep默认加unicode, case insensitive
更新技能用的 Frontmatter parser
加入异步子代理

删除 Shell、CodeRunner、ZoomIn 工具集
- 根据文件系统自动检测命令能力，并 fallback 到 JS 沙箱
- Shell 同时启用 SetTimeout 模型可以等时间。
- 对虚拟文件系统自动启用上传下载工具。
- 对具有视觉能力的模型自动启用图像检视工具

**实现文件系统挂载点，可以将文件系统挂载在 ~/ 上**
- 重新设计了记忆工具
- 加入技能系统（自发现）
- 加入挂载点管理工具
- 优化Glob和Grep工具，加入AI可见的时间筛选和数量限制
- 实现跨挂载点的文件移动
- 加 overwrite 参数防止意外覆盖

**加入上下文压缩工具集**
参考 https://github.com/complexthings/pi-dynamic-context-pruning
但基于伪造工具调用，避免 [leakage](https://github.com/complexthings/pi-dynamic-context-pruning/issues/5)，并优化、删除不需要的功能
命令： /dcp
- 命令之后可能会删，这是 GUI 应用

Compress 工具的 undo 函数尚未测试
- 不得修改 Compress 工具引用的前缀消息，DCP设计的时候可没考虑到AiChat什么都能改
- 目前 Compress 工具的参数会在数据库里存放两份，有个tricky但是暂时还不想用的方案，反正也就多几KB

### MCP

修复大量和MCP服务器相关的问题
MCP服务器连接失败时不允许自动继续对话。
后端支持通过插件注册MCP服务器，并提供几个插件（在前端添加MCP服务器来使用）
- Example: 示例，并提供一个下载文件到本地的工具
- Weather: 查询天气预报和 AQI
- EventDriver: 提供“等待”能力，让 LLM 能等待事件发生，而不需要花钱轮询
- CodeGraph: 使用 tree-sitter 构建项目的依赖关系图，减少模型读取无关文件

## 后端
日志查看器加入内存缓存
给message和log字段加入智能压缩(GPT5.6 Luna)
修复CORS Preflight不缓存的问题
修复混合使用服务器和客户端时间导致TPS计算异常的问题
加ripgrep接口，不在前端拼命令行，这样数据库模式也能安全执行 rg 程序
SSE Proxy 加入 socks5 代理支持 - 设置 proxy property 即可
修复SSE Proxy出错时JSON消息格式错误的bug
使用Radix Tree Matcher优化路由性能
命令执行：自动去除ANSI转义序列，超时加长，内联描述，精简提示词，优化环境检测代码
修复导出数据和流量节省优化冲突的问题
消息同步支持自动重连和刷新，不再需要自己判断情况点击按钮
- 刷新可能导致输入框中未提交内容被清空
向量数据库，支持可选 faiss-node 并加入 HTTP API
压缩格式版本号 10 => 11 (移除 bm_dummy 加入 parent)
协议版本号 2 => 3 (新版分支对话不兼容旧版)
修复文件读写的路径穿越漏洞（无法被AI触发，需要有效的PAT）

## UI
修复虚拟列表的部分问题(GPT5.6 Sol)
修复没有思考和内容时一直渲染loading的问题
加入左右分栏聊天界面主题
遵守 prefers-reduced-motion
支持渲染Write/Append/Edit的差异
添加新的 Loading 动画
支持编辑合并工具消息
修复输入框的圆角问题
优化一些按钮样式
优化弹窗大小
修复移动端工具配置菜单显示不全的问题
修复了置顶消息有时不置顶的bug
前端文件系统Handle支持持久化，不再需要刷新页面之后重新选择目录 (保存最近10个打开的目录)
修复重复弹出【配置同步失败】、【继续意外中断的请求】toast的问题
优化文本附件样式
修复移动端无法拖动排序的问题
优化按钮处理：修正工具无内容时输入框有内容也不能提交消息
增加不自动提交选项

## 配置
加入阿里百炼的思考和预算参数
max_tokens改max_completion_tokens
上滑隐藏输入框按钮删掉，改成移动端自动开
加入唤醒锁功能，生成对话时，在移动端亮屏，在PC端阻止睡眠
禁用mermaid插件时自动从系统提示中删除相关内容
将部分系统提示内联到工具描述中以避免子代理迷惑
provider不再使用预设名，直接用endpoint的host

## 其它
修复重放队列的序列化问题
复制对话会在标题后面加另存为
修标题生成和工具调用的兼容性问题（其实大概不算，但我想做得更好）这会更好吗我也不知道
修复可能导致消息排序错误的问题
加入工具自动修复机制（基于代码而不是模型反馈），改变调用历史
子代理完整继承文件系统
加入给grep用的.ignore文件
修复子目录的git不隐藏的问题
工具排序避免相同工具下缓存失效
修复工具调用中止时不发出声音的问题
日期现在只精确到月份避免缓存失效
加入 {{language}} 占位符，但自身未使用
修复不自动保存回落标题的问题

修复提交不支持的文件导致构造空消息的问题
修复了系统提示未标注allowedTools时会启用工具的问题
修复子代理消息渲染问题（抽取状态管理逻辑）
GetTime工具集和window上的AiChatAPI对象默认启用
修复非HTTPS环境报错的问题

2.23.0 (7/7)

休息几天，再修修bug可以开始做3.0和RPG管线了。（FINALLY！）

### 前端

重构了虚拟文件系统的实现，现在可以更方便的挂载节点了，同时持久化了临时文件系统到OPFS
加了/continue指令强行继续消息，部分解决了语义问题
- 重新生成按钮：总是重新生成
- /continue：总是继续生成
- 发送按钮：继续生成如果：主动点击停止 && 设置支持继续生成

加入子代理支持，AI可以生成子代理执行任务并获取响应
- 基础功能正常，但对话数据可能出错
- 未来可能breaking change

MCP服务器支持自定义请求头以及StreamableHTTP传输协议
- 修复无法添加MCP服务器的bug
- 优化MCP服务器连接时机
- 优化MCP格式和使用体验（breaking，你需要删除并重新添加之前的MCP服务器）

加入清空OPFS选项
toolGroup改名toolset
修改LineEdit工具名称
优化RollDice和CodeRunner工具
优化开启新对话函数的语义
为预填充状态（仅llama.cpp）加入进度指示器
优化标题生成，在工具调用链结束之后生成
优化提示词override默认工具调用配置。删掉tool_choice注入支持
优化tooltip

fix json editor(toolset) / schema
fix cmd parse
fix JsonEditor(component) 不用morphdom了有换行的边缘问题
修复无法编辑图像消息中的文本的问题
修复平均TPS计算公式的错误
修复导出对话右键菜单无效的问题

删掉一些updateStatusText的调用
修复（？）EditLines的问题
修复了思考预填充的bug

### 后端

所有LLM调用都支持日志记录了
预置字典版本升级到10
优化SSE Resume流程

2.22.4 (7/3)

### 前端

加入了支持ESM的WebWorker安全沙盒
> 未来的插件、提示词、复杂的角色卡，可以像 Node.js 项目一样分多个文件编写，打包成 ZIP 分发。
- 声明式权限管理：网络、存储、WASM

修复了某些情况下日志展示报错的问题
修复了某些情况下消息不同步的问题
修复CSS分块导致丢样式的问题
修复SSE断线重连导致UI渲染错误/丢失工具调用块的问题
optimize type definition 方便之后实现Config子类型传递到api-request
修复更新检查失败没有retryAfter耗尽请求额度的问题
修复分支管理的一个bug
加入translator管线并修改示例，优化了CSS编写方式
/say 改名 /turn

### 通用/工具

修复模型能力检测代码的bug
修复Edit工具的replaceAll不起作用的问题
修复ReadImage工具的问题
修复Grep工具的输入必须是目录/未处理换行/未格式化响应的问题
加入ignoreToolError设置，让LLM自己修复工具调用的错误
删掉HashLine，因其没什么收益，换成了MultiEdit工具，需要手动开启
使用ASCII控制字符作为行号和错误分隔符，解决了部分提示词注入和缩进混淆问题，同时不带来任何额外的token。
加入人类工具示例
修复工具调用强制审批模式下部分要求交互的工具不渲染UI的问题
修TaskList不更新的问题
修Shell超时无效的问题
修复自动审批工具设置*不生效的问题
- 支持设置工具手动审批

修复加载二进制文件时报错不够清晰的问题
加入了gitignore/ignore支持
- 仅支持根目录 修改实时应用
- 知道路径时只读
- 不是安全保证，ignore本身可被修改

OPFS文件系统支持设置子目录
等待工具调用时加入setTimeout和beforeunload的hook防止刷新时数据丢失

### 后端

修复windows下部分命令执行时的乱码问题
允许跳过日志记录
修复DB串行化的问题，在改了异步压缩算法之后出的问题
修复局域网内无法通过HTTP访问的问题
修改启动脚本修复bash乱码问题（需要测试）


## 2.22.0 (6/25)

### 破坏性更改

工具名称全部改为 PascalCase
> 之前版本所有带工具的对话都无法在新版本中继续
> 你仍然可以查看它们

JSON路径由 dot path 改为 JSON Pointer (RFC 6901) + append extension
> 具体看 JSON Editor 工具和 UpdateVariable 工具全新的语法
> 彻底解决了之前模型不用和瞎用的问题
> 设置选项中的 dot path 也需要你手动改成 JSON Pointer
> 它们会自动标红

### 前端

实现PWA，现在你可以把AiChat安装为PWA了
- 安装之前，请把 设置 > 数据 > 远程文件缓存 打开
- assets 是实时缓存，而非预缓存，所以离线使用时异步模块可能无法加载，如 mermaid

单选框使用分段按钮（琴键开关），大概更容易看出这是单选  
全局工具调用批准现在可以指定工具名称，之前只能全部允许  
工具调用块支持自定义标题，例如 ReadFile 会显示文件名  
支持读写JPEG注释。酒馆只支持PNG角色卡，这是为未来角色卡嵌入JPEG预留的API，而不是扩展兼容性

优化标题生成
- 强制JSON
- 优化设置标题工具的提示词
- 支持清除对话标题

优化了日志查看页面的操作
修复了JSON编辑器的性能问题，现在可以流畅的编辑百K级别的JSON，基本满足日常所需

加入了 Grep Append 等全新文件操作工具
- 在虚拟文件系统上进行这些操作，不依赖本机程序

修改了 SetTimeout 的功能
- 它现在更类似QTE，而不是在发送消息时告诉LLM现在几点了
- 当然，你是上帝，所以其实有不少方法能绕过它，我也wontfix

加入了工具、技能、MCP管理抽屉
> 功能越来越多，过段时间我可能又要重构设置窗口了……
> 目前，你需要在工具按钮上右键(PC)或长按(移动端)来打开管理界面

- 管理模型在当前对话中能“激活”和“使用”的工具
- 你可以添加任意MCP服务器，每个服务器将注册为模块，你可以让模型自行激活或在UI中手动管理
- 设置【新对话的默认值】需要通过系统提示的高级语法（在文档中）
- 加入只读文件访问工具集（手动开启）
- 删除了 use_tool, revoke_tool, list_tools 命令
- 记忆和追问工具不再需要靠命令激活
- 删除了 registerDefaultTool API，注册工具现在必须通过 registerTools 函数
- 增加了独立的 Skill 工具用于激活技能，通过 registerSkill 函数

加入了插件系统及UI
- 之前版本插件只能在构建前开关
- 现在可以通过UI开关（刷新页面生效）

### 插件

加入了一些插件，它们都是默认关闭的，你需要手动启用：

【TaskList、结构化任务】
- 显示和操作待办清单，主要是给人看的

【RPG管线Lite】
- /say 命令移动到这里

【JSON编辑工具】
- 基于约束采样，让LLM编辑JSON永远不出现语法错误
- 提供基于JSON Pointer的增量更新工具
- 删除网页版JSON Editor中所有的AI功能
- 支持根据 schema 文件验证 json 文件
- 支持 json5: 可以用单引号，可以裸键名，可以用注释以及各种计数法

【交互式文件上传/下载】
- 手动上传文件给LLM
- 打包下载任意文件系统中的文件
- （终于可以操作OPFS的数据了！）

【外部插件API】
- 终于有插件API了
- 你不需要编译也可以通过 `window.AiChatAPI` 注册自己的工具、渲染器、命令等
- 尽管如此，我不保证任何形式的向后兼容

### 后端
修复WS重连时锁定状态没有更新可能导致数据丢失的问题  
修复分支对话不同步的问题  
新增后台进程工具，可以在后台运行程序，如 npm run dev，不阻塞LLM

## 2.19.1 committed on 2026/6/18 星期四 at 2:46

### 前端
修复WebFileSystem的性能问题  
MD渲染器能尝试关闭层次不正确的HTML标签  
修复文档界面行内代码边框消失  
禁止换行的输入框不再使用textarea  
修复删除消息时不更新对话修改时间的问题  
部分自定义选项中的配置不再同步，适合多端  

优化Agent文件系统的选择
- 现在支持每个对话分别设置文件系统实现，而不是存在设置里
- 加入致死量的中二文案，如`今之所择为 ${folderName}，来日亦当如是。`

### 后端
- SSE Proxy 支持代理非流式请求
- KV和KV列表支持同步了（壁纸、置顶、预设、角色卡等）
- 总体配置不同步（这是特性），只会在切数据库后端的时候同步

## 2.19.0 committed on 2026/6/16 星期二 at 6:06

### 前端
- 修复移动端输入框消失的padding和其他样式问题
- 修复一个我也不知道为什么能触发的内存溢出bug
- 修复之前的tricky code（markdown块的key是固定的）导致无法更新的问题
   - 现在用更tricky的方式，但失败安全
- 修复取消登录后无法修改设置的问题
- 加入模型能力测试
   - 不再需要翻文档、研究字段、等我更新模型配置模板
   - 输入 300-3000 token (根据模型能力), 输出 ~300 token
   - 花几分钱调用几次就能测出模型能力
- 删除模型配置模板，只保留自动填充API地址功能

### 后端
- 交互式登录增加上下文（IP/设备信息）辅助决策。
- 同步服务加入PAT校验和Msgpack压缩
- 配置文件可以修改同时打开的DB数量
- 数据库模式后端不再允许运行命令，必须使用容器模式
- batch HTTP请求自动触发消息同步
   - 不需要额外通过WebSocket发送一次了
   - 减少带宽，同时防止出错：只有后端入库的请求才会被同步
- WebSocket数据包大小限制降到4KB

通信协议版本 => 3  
预置字典版本 => 9

## 2.18.0 committed on 2026/6/15 星期一 at 5:49

### 前端
- 修改可能误解会丢失数据的文本“清空数据库服务”
- 修复移动端日志表格溢出的问题，加了滚动条
- 修复对话分支状态恢复时的严重bug
- 根据模型能力改变附件的接受类型，而不是上传了才说不行

### 后端
日志接口(/logs)只返回前端渲染的数据
- 单次返回上限提升到5000条（后续可以考虑自动分块+缓存）  
  默认不再返回message_id和request_id，后者几乎是随机数，响应大小减半
- 展开查看时通过 batch 接口的 log/by-rowid 函数查询

diff/patch算法
- 支持字符串补丁，针对 部分修改文本
- 加入幂等保证，不再担心请求重放问题

阻止在data行上创建和其它行相同名称的字段，以避免混淆和冲突
- 防止未来出问题

通信协议版本 => 2: 新接口 log/by-rowid  
预置字典版本 => 8: log 接口新字段 rowid

### 安卓
- 修复状态栏颜色问题

## 2.17.0 committed on 2026/6/14 星期日 at 6:11

### 前端

UI
- 修复不创建空白预设无法开始RP的问题
- 加入了主题设置，支持跟随系统/白天/黑夜主题  
  优化了白天主题的使用体验，但可能仍有一些对比度低

- Agent: 文件读写和命令执行都使用相对路径，避免LLM混淆
- 虚拟文件系统: 修复glob的bug

- 工具调用: 修复模型可以调用未激活的工具的问题  
 只要他知道（猜到）名称

### 后端

切换数据库服务时会自动清除PAT（避免PAT泄露给新服务）
- 切换到IndexedDB时不会清除PAT

Agent
- 加入shell工具以调用系统shell，避免模型不知道怎么在arguments里面用管道符
- 在Windows上优先使用bash，其次powershell
- 修复找不到bash和ripgrep的问题
- 修复替换工具吞掉换行符的问题
- 命令执行类工具正确按时序追加stdout和stderr
- 超过20KB的命令输出会被重定向到文件，而不是一股脑丢给Agent

### 安卓
不再隐藏系统通知栏

## 2.16.0 committed on 2026/6/13 星期六 at 5:05

### 前端
修复工具提示词被替换成[object Promise]的问题  
修复顶部标题不跟随外部修改的问题  
优化性能（不再遍历对话列表给它们加ready=false，由于之前的重构，这个workaround已经没有意义）  
优化了对话加载和卸载回调，支持清理短暂数据（主要是skills/tools加的），优化内存  

预设快速切换器隐藏下划线开头的预设
- 你可以在系统提示词里用`{{:预设名称}}`引用其它预设的系统提示词
- 你可以在标题生成模型里填写`:预设名称`引用其它预设的完整配置

对话数据支持内存缓存
- 对话数据只包含文本。无论数据库后端，Blob都已分离，没有任何地方使用Base64字符串

RP:
- 支持覆盖{{char}}宏
- 修复Schema验证过于严格导致根本无法导入备份的角色扮演数据的问题
- 修复备份时的文件名问题

### 后端
日志现在有时间戳  
修复了一些乱访问Buffer内存的问题  
KVList支持差量更新  
加入通信协议版本号机制
- 该版本前的后端均为Legacy，并且不兼容
- 确保版本不统一的前后端不能交互或导致数据丢失

加入可选的基于PAT（个人访问密钥）的交互式登录
- 开启后，新客户端加入需要在控制台输入 `/accept 配对码` 确认
- 生成的Token可以按签发时间、用户作废
- 配置文件保存服务器盐值
- 用户KV表[key=salt]保存用户盐值
  - 用户可以读取、修改甚至删除它，这不是漏洞
  - 修改盐会导致相同用户的所有token立刻失效，a.k.a. 冻结账号

### 构建
PC的默认后端构建现在使用brip格式嵌入前端
- brip为zip-io库支持的br压缩的zip，其method=92
- 服务端支持显示前端版本了

强烈建议使用https+自签名证书，即便是内网。（当然，有域名更好）  
安卓版本后端必须https，我信任了用户证书。

## 2.15.1 committed on 2026/6/12 星期五 at 6:02

### 前端
修复移动端音乐播放器进度条没有宽度、点击下载会拉起系统浏览器的问题
优化同步服务重连，不再需要刷新页面
- 优化同步性能
- 重连时使用最新对话时间戳进行可选更新，基本上来说，只要断线期间没有导入过去的数据就行

对话加载时防止进行敏感操作，优化弱网体验，防止意外数据损坏
- 现在初始化时也从服务器拉取配置

### 后端
用户数据版本 -> v2，自动迁移
增加了time索引，适配`If-Modified-Since`带宽优化

### 安卓
换了虚拟域名，不再使用 `file://`   
**更新前自行在设置内导出所有数据。**
